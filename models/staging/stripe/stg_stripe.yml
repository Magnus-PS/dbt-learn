version: 2

models:
  - name: stg_payments
    description: Staged payment data from jaffle shop app.
    columns:
      - name: payment_id
        description: primary key for stg_payments.
      - name: order_id
        description: id assigned to order for corresponding stg_payments.